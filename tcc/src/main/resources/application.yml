spring:
  application:
    name: fraud-api

  datasource:
    url: jdbc:postgresql://localhost:5432/fraud_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

dataset:
  folder-path: ${DATASET_PATH:/Users/pablo/Documents/java-projects/unip-tcc}

server:
  servlet:
    context-path: /process_fraud_automotive
